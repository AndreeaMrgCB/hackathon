# hackathon

## Setup backend:

first setup the environment:
> cd backend
> py -3 -m venv .venv

then activate the environment:
> .venv\Scripts\activate

then installing flask:
> pip install Flask

to run the app call:
> python app.py